# Private Example Configs

This directory is for private-memory scan configuration examples only.

Do not run these configs against non-anonymized personal or production data.
Use sanitized or synthetic datasets before publishing artifacts derived from scans.
