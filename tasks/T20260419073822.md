---
type: task
project: mdex
status: done
updated: 2026-04-19
---

# Task: mdex 公開 hygiene 一括: scan_config サニタイズ／.mdex/ 集約／.gitignore 強化／docs パス正規化

**ID**: T20260419073822
**Project**: mdex
**Priority**: high
**Status**: done
**Created**: 2026-04-19T07:38:22.714127+00:00

## Description

タスクファイル: C:\Codex\infra\tasks\pending\{ID}.md
実装先: C:\Codex\mdex

## 背景
2巡目の公開監査で、ライセンス／CI／schema／path containment は合格したが「公開用 default の hygiene」が残課題と判明。内部絶対パスと任意名出力物の再コミット事故を防ぐため、ここを一括で締め直す。

## 対象ファイル
- control/scan_config.json（`scan_roots: ["C:/Codex"]` → `["."]` or 省略、`output_file` を `.mdex/mdex_index.json` に変更、exclude_patterns に `.mdex/**` を追加）
- control/yura_memory_scan_config.json → `examples/private/yura_memory_scan_config.json` へ移動。移動先に README を1本添えて「匿名化済みデータ以外を食わせるな」と明示する警告文を置く
- .gitignore を以下方針へ強化: `.mdex/`, `outputs/`, `tmp/`, `*.db`, `*.sqlite`, `*.sqlite3` を ignore。既存の `/*_index*` 系は残してよい（互換）
- runtime/cli.py / runtime/indexer.py / runtime/dbresolve.py: `scan` の既定出力と DB 既定探索先を `.mdex/` 配下に寄せる（環境変数・CLI引数で上書き可能は維持）。repo_default 既定順序を `.mdex/mdex_index.db` → `mdex_index.db`（互換のため fallback）
- docs/phase_a_agent_flow.md: `C:/work/...` `C:/Codex/mdex/...` 等の実環境風パス例を `<repo-root>/...` 系へ正規化
- README.md: 既定 DB/出力 path の説明、Quick Verification、サンプル、.gitignore 方針、公開 scan_config の使い方を `.mdex/` 集約に合わせて更新
- tests/ 配下: 既定 path 変更の影響を受けるテストを更新（既存テスト緑を維持）

## 完了条件
1. scan_config.json に絶対パスがなく、`scan_roots` が `"."` もしくは省略
2. scan_config.json の `output_file` が `.mdex/mdex_index.json`
3. yura_memory_scan_config.json が examples/private/ に退避され、当該 README に警告文あり
4. .gitignore が `.mdex/` ディレクトリを確実に ignore している
5. `mdex scan --root .` を引数なし DB で実行すると `.mdex/mdex_index.db` と `.mdex/mdex_index.json` に既定で出力される
6. `mdex start` / `mdex context` の DB 自動解決で `.mdex/mdex_index.db` が最優先で見つかる
7. docs/phase_a_agent_flow.md に `C:/` 始まりの実パス例が残っていない（grep でゼロ件）
8. README の既定出力例・サンプル・Quick Verification がすべて `.mdex/` 集約を反映し再現可能
9. 既存 69 passed を維持。contract test / readme examples test も green
10. pytest green

## 非目標
- 出力 JSON/schema の破壊的変更
- 既存 index DB からの移行スクリプト提供（ユーザーが手で作り直せばよい）
- error schema 追加（別タスク）

## 着手前
- `codegraph context runtime/cli.py` / `codegraph context runtime/dbresolve.py` で DB 解決経路を把握
- 既存 tests/test_phase_a_cli.py / test_readme_examples.py の期待 path を先に棚卸し

## Result

scan_config サニタイズ (scan_roots → ["."])、yura_memory_scan_config.json を examples/private/ に退避＋警告 README、.gitignore 強化 (.mdex/ outputs/ tmp/ *.db *.sqlite*)、既定出力を .mdex/ 配下へ、docs/phase_a_agent_flow.md の C:/ 実パス例を全除去。commit 1214992 push済。
