---
type: task
project: mdex
status: done
updated: 2026-04-19
---

# Task: [mdex] H: update_node_summary source 引数 + scripts/ 削除

**ID**: T20260419125443
**Project**: mdex
**Priority**: low
**Status**: done
**Created**: 2026-04-19T12:54:43.381608+00:00

## Description

タスクファイル: `C:\Codex\infra\tasks\pending\{ID}.md`
実装先: `C:\Codex\mdex`

## 目的
小さな API 不整合を掃除する:
(1) `update_node_summary(source="agent")` が引数値を無視して固定値を使っている
(2) `scripts/mdex_start.py` / `scripts/mdex_finish.py` が `project.scripts` に未登録で位置づけ不明 → `mdex start` / `mdex finish` CLI で代替可能なら削除

## 対象ファイル
- `runtime/store.py::update_node_summary`
- `scripts/mdex_start.py`, `scripts/mdex_finish.py` (削除候補)
- 呼び出し元(`runtime/cli.py::_cmd_enrich` 他、`source` 引数の実渡し確認)
- README / docs の scripts 参照箇所
- `tests/test_store.py` (source 引数挙動 test)

## 完了条件
- `update_node_summary`: `source` 引数の値を実際に `UPDATE nodes SET summary_source = ? ...` で使う。`"agent"` / `"scan"` / 他の値を判別して保存
- 呼び出し元の source 渡しを意味が正しくなるように修正(enrich では "agent"、scan では "scan" 等)
- `scripts/mdex_start.py` / `scripts/mdex_finish.py` の挙動が `mdex start` / `mdex finish` CLI で完全に代替可能か**Codex が検証**:
  - (a) 代替可能 → 両 script を削除、README / docs の参照を `mdex start` / `mdex finish` に置換、CHANGELOG に breaking 明記
  - (b) 代替不能 or 固有機能あり → 削除せず examples/ に移動して位置づけ明記(この場合は完了時にユーザー通知)
- regression test: source 引数の値が DB に保存されることを assert
- `pytest -q` all green

## 非目標
- `summary_source` column の新規追加(既存 column があれば活用、無ければ Codex 判断で migration 追加)

## 着手前
- `C:\Codex\deep-research-report.md` の H の所見を参照
- `scripts/mdex_start.py` と `scripts/mdex_finish.py` の中身を Read して CLI 代替可能性を判定

## Result

update_node_summary の source 引数を実使用するよう修正。scripts/mdex_start.py と mdex_finish.py は mdex start/finish で代替可能確認、削除。
