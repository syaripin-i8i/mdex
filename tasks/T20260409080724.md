---
type: task
project: mdex
status: done
updated: 2026-04-09
---

# Task: quality_repo の golden path を CLI end-to-end で固定する

**ID**: T20260409080724
**Project**: mdex
**Priority**: high
**Status**: pending
**Created**: 2026-04-09T08:07:24+00:00

## Description

実装先: C:\Codex\mdex\tests\
タスクファイル置き場: C:\Codex\infra\tasks\pending\

## 目的

関数レベルの fixture テストはかなり揃ってきたので、次は README に書いた利用フローを「実コマンドで壊れない」状態に固定したい。`tests/fixtures/quality_repo` を使って、`scan -> first -> related -> enrich` を `python -m runtime.cli` 経由で 1 本通す end-to-end テストを追加する。

## 対象ファイル

- `C:\Codex\mdex\tests\test_mdex.py` または `C:\Codex\mdex\tests\test_cli_e2e.py`
- `C:\Codex\mdex\tests\conftest.py`（helper が必要なら）

## 事前に確認できている真値

- `scan --root tests/fixtures/quality_repo --config control/scan_config.json`
  - `nodes = 6`
  - `edges.total = 8`
  - `edges.resolved = 6`
  - `edges.unresolved = 2`
- `first design/root.md --limit 2`
  - `prerequisites[0].id = "spec/b.md"`
  - `prerequisites[1].id = "decision/a.md"`
- `related design/root.md --limit 3`
  - `related` の先頭 3 件は
    - `decision/a.md`
    - `spec/b.md`
    - `tasks/T20260101000001.md`
- `enrich design/root.md --summary "..."`
  - `status = "enriched"`
  - `summary_source = "agent"`
  - `skipped = false`

## 完了条件

- 実コマンド `python -m runtime.cli ...` を subprocess で呼ぶ E2E テストを追加する
- 1 つのテスト内で `scan -> first -> related -> enrich` を順に実行し、主要 payload を assert する
- timestamp や全文 JSON を丸ごと固定するのではなく、node id / 順序 / status / resolved 件数など壊れやすい核だけを固定する
- `python -m pytest -q` が通ること（現状 baseline: `42 passed`）

---
## 完了手順（Codex 定型）

### パス情報
- このファイル  : `C:\Codex\infra\tasks\pending\T20260409080724.md`
- 完了後の移動先: `C:\Codex\infra\tasks\done\T20260409080724.md`
- プロジェクト  : `C:\Codex\mdex`

### 環境制約
- `rg`（ripgrep）は権限エラーで使用不可
- ファイル検索 → `Get-ChildItem -Path <絶対パス> -Recurse -Filter <pattern>`
- 文字列検索 → `Select-String -Path <絶対パス> -Pattern <pattern> -Recurse`
- パスは常に絶対パスで指定すること（`C:\Codex\...` 形式）

### 完了時の操作
1. このファイルに Result セクションを追記する
   ```
   ## Result
   
   - 実施内容: （何をしたか）
   - 確認方法: （どう検証したか）
   - 備考: （気づいたことがあれば）
   ```

2. ファイルを done/ に移動する
   ```powershell
   Move-Item 'C:\Codex\infra\tasks\pending\T20260409080724.md' 'C:\Codex\infra\tasks\done\T20260409080724.md'
   ```

3. DB 更新は不要（Claude 側が done/ を確認して取り込む）
## Result

- 実施内容: `tests/test_cli_e2e.py` を新規追加し、`python -m runtime.cli` 経由で `scan -> first -> related -> enrich` を 1 テスト内で順次実行して、`nodes=6`、`edges=8/6/2`、`first` 順序、`related` 上位3件、`enrich` の `status/summary_source/skipped` を固定しました。
- 確認方法: `python -m pytest -q tests/test_cli_e2e.py`
- 備考: `scan` は `--root tests/fixtures/quality_repo --config control/scan_config.json` の実コマンドで実行しています。
