---
type: task
project: mdex
status: done
updated: 2026-04-09
---

# Task: mdex full-scan: C:\Codex 全Markdown紐づけのための改善と全量索引化

**ID**: T20260409035943
**Project**: mdex
**Priority**: high
**Status**: pending
**Created**: 2026-04-09T03:59:43+09:00

## Description

**対象**: `C:\Codex` 配下の Markdown 全体  
**実装先**: `C:\Codex\mdex\runtime\`

## 目的

- `C:\Codex` 配下の markdown を可能な限り相互参照で紐づける
- 必要に応じて parser/builder/scanner のリンク解決精度を改善
- 全量 index（JSON + SQLite）を生成する

## 実装対象

- exclude 判定の強化（ネストされた `.git/`, `node_modules/` など）
- markdown 内のパス参照抽出強化（特に task 文書での実パス記法）
- 曖昧リンク（同名ファイル）解決のヒューリスティクス改善

## 完了条件

- `python runtime/cli.py scan --root C:/Codex --output mdex_codex_index.json --db mdex_codex_index.db` が成功
- ノード/エッジ統計と内部解決率を提示できる
- 改善内容と結果を本タスクに `Result` として記録

---
## 完了手順（Codex 定型）

### パス情報
- このファイル  : `C:\Codex\infra\tasks\pending\T20260409035943.md`
- 完了後の移動先: `C:\Codex\infra\tasks\done\T20260409035943.md`
- プロジェクト  : `C:\Codex\mdex`

### 完了時の操作
1. このファイルに Result セクションを追記する
2. ファイルを done/ に移動する
3. DB 更新は不要（done/ を取り込み）

## Result

- 実施内容: `C:\Codex\mdex\runtime\scanner.py` の exclude 判定を強化し、`**/.git/**` 等のネストパターンにも一致するよう改善。`exclude_patterns` の既存指定（`.git/**`, `node_modules/**`）でも深い階層を除外可能にした。
- 実施内容: `C:\Codex\mdex\runtime\parser.py` を拡張し、`path_refs`（inline code や Windows 絶対パスの `.md` 参照）を抽出。`task_refs`/metadata fallback と併用して task 文書の参照を拾えるようにした。
- 実施内容: `C:\Codex\mdex\runtime\builder.py` を再構成し、`scanner` 利用・root基準ID統一・同名ファイル曖昧解決ヒューリスティクス（同トップ階層/共通prefix優先）・テンプレート記法ノイズ除去（`{ID}`/`.md` 等）を追加。
- 実施内容: `C:\Codex\mdex\runtime\cli.py` の `open` に `--index` を追加。index の `scan_root` から自動解決できるようにした（全量indexで `open` が使いやすくなる）。
- 確認方法: `python -m py_compile runtime/scanner.py runtime/parser.py runtime/builder.py runtime/indexer.py runtime/reader.py runtime/cli.py` を実行して成功。
- 確認方法: `python runtime/cli.py scan --root C:/Codex --output C:/Codex/mdex/mdex_codex_index.json --db C:/Codex/mdex/mdex_codex_index.db` 実行で成功。
- 確認方法: 出力統計を確認し `nodes=442`, `edges=328`, `internal_edges=281`（内部解決率 85.67%）を確認。SQLite 側も `nodes=442`, `edges=328` で一致。
- 確認方法: `python runtime/cli.py query --index C:/Codex/mdex/mdex_codex_index.json --node infra/tasks/T20260330022925.md --depth 1` で task 関連ノードが辿れることを確認。`python runtime/cli.py open infra/tasks/T20260409035436.md --index C:/Codex/mdex/mdex_codex_index.json` で本文表示を確認。
- 備考: 生成ファイルは `C:\Codex\mdex\mdex_codex_index.json` / `C:\Codex\mdex\mdex_codex_index.db`。
