---
type: task
project: mdex
status: done
updated: 2026-04-12
---

# Task: mdex Phase A 出口(後半) - `finish` 本体と `impact` コマンド

**ID**: T20260412190920
**Project**: mdex
**Priority**: normal
**Status**: pending
**Created**: 2026-04-12T19:09:20+00:00

## Description

**タスクファイル**: `C:\Codex\infra\tasks\pending\T20260412190920.md`
**実装コミット先**: `C:\Codex\mdex`

## 参照設計

- `C:\Codex\mdex\docs\phase_a_agent_flow.md`

## 依存関係

- `T20260412190919`

## 背景

dry-run だけでは終了導線として弱い。
changed files から設計書/決定記録/タスクを寄せる `impact` を公開し、`finish` では `summary-file` と `scan` を扱える本体まで仕上げる。

## 変更内容

### 1. `mdex impact` の追加

新コマンド:

```bash
mdex impact <path> [<path> ...]
mdex impact --changed-files-from-git
```

返却カテゴリ:

- `read_first`
- `related_tasks`
- `decision_records`
- `stale_watch`

### 2. `finish` の apply モード

以下を追加する。

- `--summary-file <path>`
- `--scan`

仕様:

- Primary 候補が 1 件に絞れる場合のみ `enrich` を適用
- それ以外は `requires_manual_targeting=true` で更新しない
- `--scan` 指定時のみ最後に再 scan を行う

### 3. 出力 JSON の最終化

最低限以下を返す。

- `applied_enrichments`
- `requires_manual_targeting`
- `scan.requested`
- `scan.ran`

## 対象ファイル

- `C:\Codex\mdex\runtime\cli.py`
- `C:\Codex\mdex\runtime\finish.py`
- `C:\Codex\mdex\runtime\impact.py`
- `C:\Codex\mdex\runtime\enrich.py`
- `C:\Codex\mdex\tests\test_cli_e2e.py`
- 新規または既存 impact/finish テスト

## 完了条件

1. `mdex impact runtime/emotion.py` 相当の path 入力が valid JSON を返す
2. `mdex impact --changed-files-from-git` が git 変更起点でカテゴリ分類を返す
3. `mdex finish --task "..." --summary-file <path>` が Primary 1 件時のみ `enrich` を適用する
4. `mdex finish --task "..." --scan` が再 scan 結果を JSON に反映する
5. `python -m pytest` で既存 `context` / `enrich` 回帰が落ちない

## 備考

- Phase A では意味推定より path / link / frontmatter 一致を優先する
- 曖昧な multi-write は避け、保守的に止まる実装でよい

---
## 完了手順（Codex 定型）

### パス情報
- このファイル  : `C:\Codex\infra\tasks\pending\T20260412190920.md`
- 完了後の移動先: `C:\Codex\infra\tasks\done\T20260412190920.md`
- プロジェクト  : `C:\Codex\mdex`

### 環境制約
- `rg`（ripgrep）は権限エラーで使用不可
- ファイル検索 → `Get-ChildItem -Path <絶対パス> -Recurse -Filter <pattern>`
- 文字列検索 → `Select-String -Path <絶対パス> -Pattern <pattern> -Recurse`
- パスは常に絶対パスで指定すること（`C:\Codex\...` 形式）

### 完了時の操作
1. このファイルに Result セクションを追記する
   ```
   ## Result
   
   - 実施内容: （何をしたか）
   - 確認方法: （どう検証したか）
   - 備考: （気づいたことがあれば）
   ```

2. ファイルを done/ に移動する
   ```powershell
   Move-Item 'C:\Codex\infra\tasks\pending\T20260412190920.md' 'C:\Codex\infra\tasks\done\T20260412190920.md'
   ```

3. DB 更新は不要（Claude 側が done/ を確認して取り込む）

## Result

- 実施内容: `runtime/impact.py` と CLI `impact` を実装し、`finish` に `--summary-file` apply モードと `--scan` 再索引実行を追加しました。Primary 1 件時のみ `enrich` を適用し、複数候補時は `requires_manual_targeting=true` で停止します。
- 確認方法: `python -m pytest -q`、`tests/test_phase_a_cli.py` の `impact` / `finish apply` / `finish manual targeting` / `finish --scan` ケースで確認。
- 備考: Phase A 方針どおり path/link/frontmatter 近接の保守的スコアで分類しています。
