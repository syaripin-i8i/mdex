---
type: task
project: mdex
status: done
updated: 2026-04-19
---

# Task: mdex error payload の schema 化（stderr JSON 契約化）

**ID**: T20260419073835
**Project**: mdex
**Priority**: normal
**Status**: done
**Created**: 2026-04-19T07:38:35.940583+00:00

## Description

タスクファイル: C:\Codex\infra\tasks\pending\{ID}.md
実装先: C:\Codex\mdex

## 背景
docs/schema_versioning.md:34 が "success payloads only" と認めているとおり、error 契約が schema 化されていない。agent-first CLI は失敗経路こそ機械可読であるべきなので、stderr JSON も契約として固定する。

## 対象
- schemas/error.schema.json を新規作成（draft 2020-12）。現行 CLI が stderr に出す error JSON の実形状を棚卸してから schema を定義すること
  - 最低限: `error` (string), `detail` (string, optional), `node_id` (string, optional), `resolution_attempts` (array of objects, optional)
  - 未知フィールドは forward-compatible に許容する方針に合わせる
- docs/schema_versioning.md 更新: error schema を scope に加え、semver・互換・deprecation 規則を success 側と同等に適用すると明記
- runtime/cli.py / 各コマンドの error 出力関数: 必要なら error 形状を schema 準拠に整える（破壊的変更は避ける。新フィールド追加のみ）
- tests/test_schema_contracts.py: 既存 success 契約テストと同じ粒度で、各コマンドの error 経路（絶対パス拒否／未索引拒否／`..` 拒否／DB 未解決／scan_root escape 等）の stderr 出力が error schema を満たすことを検証

## 完了条件
1. schemas/error.schema.json が存在し、現行 error JSON 実出力と整合
2. docs/schema_versioning.md の scope に error schema が含まれる
3. contract test が success/error の両側を網羅し green
4. 既存 69 passed を維持
5. pytest green

## 非目標
- error 文言の UX 改良
- 新しい error kind の追加（現行経路の schema 化のみ）

## 着手前
- `codegraph context runtime/cli.py` で error 出力経路を把握
- tests/test_phase_a_cli.py の containment 系テストがすでに出している error JSON を参照し、現行形状を把握してから schema を書くこと

## Result

schemas/error.schema.json 追加 (draft 2020-12)、docs/schema_versioning.md の scope に error 追加、contract test を success/error 両経路に拡張。pytest 72 passed。
