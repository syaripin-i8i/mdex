---
type: task
project: mdex
status: done
updated: 2026-04-09
---

# Task: context コマンドのスコア重み付けを説明可能にする

**ID**: T20260409071036
**Project**: mdex
**Priority**: normal
**Status**: pending
**Created**: 2026-04-09T07:10:36.214663+00:00

## Description

実装先: C:\Codex\mdex\runtime\context.py、C:\Codex\mdex\docs\
タスクファイル置き場: C:\Codex\infra\tasks\pending\

## 目的
context コマンドのスコアリングに使われる重み付け（distance / token_cost / co-occurrence 等）が、コード内に散在しており説明可能でない。「なぜこのノードがこのスコアになるか」を外部から追えるようにする。

## 完了条件
- context コマンドの出力に score_breakdown フィールド（各要素の寄与を含む dict）を追加（JSON 出力時のみで可）
- 重みの定数を context.py の先頭にまとめてコメント付きで定義する
- docs/ に context スコアリングの設計メモを 1 ファイル追加する（箇条書き程度で可）

---
## 完了手順（Codex 定型）

### パス情報
- このファイル  : `C:\Codex\infra\tasks\pending\T20260409071036.md`
- 完了後の移動先: `C:\Codex\infra\tasks\done\T20260409071036.md`
- プロジェクト  : `C:\Codex\mdex`

### 環境制約
- `rg`（ripgrep）は権限エラーで使用不可
- ファイル検索 → `Get-ChildItem -Path <絶対パス> -Recurse -Filter <pattern>`
- 文字列検索 → `Select-String -Path <絶対パス> -Pattern <pattern> -Recurse`
- パスは常に絶対パスで指定すること（`C:\Codex\...` 形式）

### 完了時の操作
1. このファイルに Result セクションを追記する
   ```
   ## Result
   
   - 実施内容: （何をしたか）
   - 確認方法: （どう検証したか）
   - 備考: （気づいたことがあれば）
   ```

2. ファイルを done/ に移動する
   ```powershell
   Move-Item 'C:\Codex\infra\tasks\pending\T20260409071036.md' 'C:\Codex\infra\tasks\done\T20260409071036.md'
   ```

3. DB 更新は不要（Claude 側が done/ を確認して取り込む）
## Result

- 実施内容: `context.py` の重み定数を先頭へ集約し、候補ごとに `score_breakdown`（keyword/type_status/recency/graph_boost/token_cost/total）を JSON 出力へ追加。`docs/context_scoring.md` を新規作成して計算式と意図を文書化。
- 確認方法: `tests/test_context.py` に score_breakdown 検証テストを追加し、`python -m pytest -q`（42 passed）。
- 備考: token_cost は順位スコアに直接加算せず、budget gate の説明用メタとして返却。
