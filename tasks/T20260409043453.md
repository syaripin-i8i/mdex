---
type: task
project: mdex
status: done
updated: 2026-04-09
---

# Task: Phase2-A3: list --json オプション追加

**ID**: T20260409043453
**Project**: mdex
**Priority**: normal
**Status**: done
**Created**: 2026-04-09T04:34:53.731317+00:00

## Description

タスクファイル: C:\Codex\infra\tasks\pending\
実装先: C:\Codex\mdex

## 目的
`mdex list` に `--format json` オプションを追加する。
CLIパイプ用途・他ツール連携用の JSON 配列出力モード。
探索機能ではなく出力インターフェース整備。

## 対象ファイル
- runtime/cli.py の `_cmd_list` と list subparser を修正
  - `--format` 引数を追加（選択肢: `table`（デフォルト）/ `json`）
  - `json` 時は `json.dumps(nodes, ensure_ascii=False, indent=2)` で出力
  - デフォルト動作（タブ区切り）は変えない

## 完了条件
- `mdex list --db <db> --format json` で JSON 配列が出力される
- `mdex list --db <db>` の既存動作が変わらない
- `python -m pytest -q` が通る
