---
type: task
project: mdex
status: done
updated: 2026-04-09
---

# Task: mdex Phase1-align-3: cli.py を scan/list/open 構成へ揃える

**ID**: T20260409033738
**Project**: mdex
**Priority**: high
**Status**: pending
**Created**: 2026-04-09T03:37:38+09:00

## Description

**実装先**: `C:\Codex\mdex\runtime\cli.py`
**背景**: `docs/design.md` の Phase 1 完了条件に合わせ、CLI の主要コマンドを `scan/list/open` として整える。

## 実装対象

- `scan`: indexer を呼び出し JSON + SQLite を出力
- `list`: index JSON からフィルタ付き一覧を表示
- `open`: 指定ノードIDの本文を表示
- 既存互換のため `query` は任意で残してよい

## 完了条件

- `python -m py_compile runtime/cli.py` が通る
- `python runtime/cli.py scan --root docs/` で JSON/DB が生成される
- `python runtime/cli.py list` が出力される
- `python runtime/cli.py open docs/proposal.md` が本文を出力する

---
## 完了手順（Codex 定型）

### パス情報
- このファイル  : `C:\Codex\infra\tasks\pending\T20260409033738.md`
- 完了後の移動先: `C:\Codex\infra\tasks\done\T20260409033738.md`
- プロジェクト  : `C:\Codex\mdex`

### 完了時の操作
1. このファイルに Result セクションを追記する
2. ファイルを done/ に移動する
3. DB 更新は不要（done/ を取り込み）

## Result

- 実施内容: `C:\Codex\mdex\runtime\cli.py` を更新し、`scan/list/open` を設計書どおり利用可能にした。`scan` は `indexer` 経由で JSON + SQLite を同時出力、`open` は `reader.py`（新規追加）を使ってノード本文を表示。既存 `query` は互換維持で残置。
- 確認方法: `python -m py_compile C:\Codex\mdex\runtime\cli.py C:\Codex\mdex\runtime\reader.py` を実行して成功。`python runtime/cli.py scan --root docs/`、`python runtime/cli.py list`、`python runtime/cli.py open docs/proposal.md` を実行し、期待どおり動作することを確認。
- 備考: `scan` の stderr 出力に JSON と SQLite の出力先を両方表示するように調整。
