---
type: task
project: mdex
status: done
updated: 2026-04-09
---

# Task: mdex Phase1-align-1: scanner.py を追加して .md 列挙を独立化

**ID**: T20260409033710
**Project**: mdex
**Priority**: high
**Status**: pending
**Created**: 2026-04-09T03:37:10+09:00

## Description

**実装先**: `C:\Codex\mdex\runtime\scanner.py`
**背景**: `docs/design.md` のモジュール構成では scanner が独立モジュールになっているため、現行の builder 内部走査を分離する。

## 実装対象

- `list_markdown_files(root: str, exclude_patterns: list[str] | None = None) -> list[str]` を実装
- 返却値は root からの相対パス（POSIX 形式）でソート済み
- `exclude_patterns`（glob）で除外対応

## 完了条件

- `python -m py_compile runtime/scanner.py` が通る
- `python - <<PY ...` で `list_markdown_files('docs')` 実行時に `design.md` / `proposal.md` が含まれる

---
## 完了手順（Codex 定型）

### パス情報
- このファイル  : `C:\Codex\infra\tasks\pending\T20260409033710.md`
- 完了後の移動先: `C:\Codex\infra\tasks\done\T20260409033710.md`
- プロジェクト  : `C:\Codex\mdex`

### 完了時の操作
1. このファイルに Result セクションを追記する
2. ファイルを done/ に移動する
3. DB 更新は不要（done/ を取り込み）

## Result

- 実施内容: `C:\Codex\mdex\runtime\scanner.py` を新規作成し、`list_markdown_files(root, exclude_patterns)` で `.md` 列挙（相対POSIXパス・ソート・除外パターン対応）を実装。
- 確認方法: `python -m py_compile C:\Codex\mdex\runtime\scanner.py` を実行して成功。`list_markdown_files('C:\\Codex\\mdex\\docs')` の結果に `design.md` と `proposal.md` が含まれることを確認。
- 備考: 除外は glob パターン（`fnmatch`）で判定。
