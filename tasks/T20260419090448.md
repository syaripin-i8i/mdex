---
type: task
project: mdex
status: done
updated: 2026-04-19
---

# Task: [mdex] README に scan 生成物の privacy note 追加

**ID**: T20260419090448
**Project**: mdex
**Priority**: normal
**Status**: done
**Created**: 2026-04-19T09:04:48.903289+00:00

## Description

タスクファイル: `C:\Codex\infra\tasks\pending\{ID}.md`
実装先: `C:\Codex\mdex`

## 目的
mdex は `.md` / `.json` / `.jsonl` の `message` / `content` / `prompt` / `response` / `assistant` / `user` / `system` / `events` / `logs` を summary 候補として拾う設計のため、chat log 等を scan 対象に含めると index に機微情報が要約される。公開 repo にこの事実を明示し誤用を減らす。

## 対象ファイル
- `README.md`(Artifact Hygiene 節の近くか Setup 直後に追加が適切)

## 完了条件
- "Privacy note" section(数行)を追加し、以下を明記:
  - scan output (`.mdex/mdex_index.json`, `.mdex/mdex_index.db`) は scan 対象ファイルの summary を含む
  - chat log / secret を含むファイルを scan すると index にもその内容が要約される
  - 公開 repo で track する前に index を必ず確認するか、`scan_config.json` の exclude で機微ディレクトリを除外する
  - `.gitignore` で `.mdex/` を無視している点への言及
- 日本語 / 英語どちらでも可(既存 README の tone に合わせる)

## 非目標
- scan 側の機微データ検出機能の実装
- `.gitignore` の変更(既に `.mdex/` 除外済み)

## 着手前
- 現行 `README.md` の章構成を Read で確認

## Result

README に scan 生成物の privacy note 追記。commit ee3bbc3。
