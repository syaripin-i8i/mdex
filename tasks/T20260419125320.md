---
type: task
project: mdex
status: done
updated: 2026-04-19
---

# Task: [mdex] setuptools>=77 昇格 + pylock.toml で依存固定

**ID**: T20260419125320
**Project**: mdex
**Priority**: high
**Status**: done
**Created**: 2026-04-19T12:53:20.882923+00:00

## Description

タスクファイル: `C:\Codex\infra\tasks\pending\{ID}.md`
実装先: `C:\Codex\mdex`

## 目的
build backend 下限を SPDX license expression がサポートされる `setuptools>=77` に揃える。さらに `pylock.toml` (PEP 751) で依存を固定し、「同じ commit を別日にビルドすると別の依存集合になる」状態を解消する。

## 対象ファイル
- `pyproject.toml` (`[build-system] requires` 昇格)
- `pylock.toml` (新規作成、PEP 751 形式)
- `.github/workflows/ci.yml` (lockfile ベースの install に切替)
- `CONTRIBUTING.md` / `README.md` (lockfile 更新手順の追記)

## 完了条件
- `[build-system] requires = ["setuptools>=77", "wheel"]`
- `pylock.toml` を生成(pip 25.2+ の `pip lock` か `uv pip compile --universal` 等、Codex が使える道具を選択)。runtime + dev 両方を含むかは判断(分割可)
- CI の test / audit / build-smoke job は lockfile install に切替(`pip install --lock pylock.toml` or equivalent)
- `pip install -e .[dev]` フォールバック手順を CONTRIBUTING に残す(開発者が lockfile 更新するとき用)
- lockfile 更新コマンドを README か CONTRIBUTING に明記
- CI 全 green(9 pytest + pip-audit + build-smoke)
- `python -m build` が通ること

## 非目標
- Release workflow の整備(別タスク T3 で対応)
- 依存バージョンの大幅 upgrade

## 着手前
- `C:\Codex\deep-research-report.md` の B の所見を参照
- pip / uv のどちらを使うかは Codex 判断。CI runner の ubuntu-latest で動くことが条件

## Result

setuptools>=77 昇格 + pylock.toml 導入。CI を lock install に切替。pip-audit vulnerabilities 0 件。
