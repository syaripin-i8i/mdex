---
type: task
project: mdex
status: done
updated: 2026-04-09
---

# Task: override を検索・順位付けに反映する

**ID**: T20260409071013
**Project**: mdex
**Priority**: high
**Status**: pending
**Created**: 2026-04-09T07:10:13.983569+00:00

## Description

実装先: C:\Codex\mdex\runtime\
タスクファイル置き場: C:\Codex\infra\tasks\pending\

## 目的
node_overrides に保存された summary が、find / related / first の検索スコアリングに反映されていない。override が「継続的に改善される探索基盤」の芯になるには、スコア計算時に override summary を通常 summary より優先して使う必要がある。

## 対象ファイル
- runtime/resolver.py（find / related / first のスコア計算）
- runtime/store.py（node_overrides の取得ロジック）

## 完了条件
- find / related / first の各コマンドで、対象ノードに override summary が存在する場合はそちらをスコアリングに使う
- override が存在しない場合は従来の summary にフォールバック
- 既存テスト 32 passed を維持

---
## 完了手順（Codex 定型）

### パス情報
- このファイル  : `C:\Codex\infra\tasks\pending\T20260409071013.md`
- 完了後の移動先: `C:\Codex\infra\tasks\done\T20260409071013.md`
- プロジェクト  : `C:\Codex\mdex`

### 環境制約
- `rg`（ripgrep）は権限エラーで使用不可
- ファイル検索 → `Get-ChildItem -Path <絶対パス> -Recurse -Filter <pattern>`
- 文字列検索 → `Select-String -Path <絶対パス> -Pattern <pattern> -Recurse`
- パスは常に絶対パスで指定すること（`C:\Codex\...` 形式）

### 完了時の操作
1. このファイルに Result セクションを追記する
   ```
   ## Result
   
   - 実施内容: （何をしたか）
   - 確認方法: （どう検証したか）
   - 備考: （気づいたことがあれば）
   ```

2. ファイルを done/ に移動する
   ```powershell
   Move-Item 'C:\Codex\infra\tasks\pending\T20260409071013.md' 'C:\Codex\infra\tasks\done\T20260409071013.md'
   ```

3. DB 更新は不要（Claude 側が done/ を確認して取り込む）
## Result

- 実施内容: `search_nodes()` を override 適用済みノードベースの検索スコア計算に変更し、`find` が `node_overrides.summary` を優先してマッチ/順位付けするよう修正。`related_nodes()` に summary overlap スコアを追加し、`prerequisite_order()` に summary を使った tie-break score を追加して `first` 出力へ `score` を付与。
- 確認方法: `python -m py_compile runtime/store.py runtime/resolver.py`、`python -m pytest -q`（42 passed）。
- 備考: summary overlap は stopword を除外してノイズを抑え、既存の edge ベース順位を壊しにくい重みに調整。
