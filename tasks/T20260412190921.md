---
type: task
project: mdex
status: done
updated: 2026-04-12
---

# Task: mdex Phase A 記録支援 - `new` / `stamp` の前倒し実装

**ID**: T20260412190921
**Project**: mdex
**Priority**: normal
**Status**: pending
**Created**: 2026-04-12T19:09:21+00:00

## Description

**タスクファイル**: `C:\Codex\infra\tasks\pending\T20260412190921.md`
**実装コミット先**: `C:\Codex\mdex`

## 参照設計

- `C:\Codex\mdex\docs\phase_a_agent_flow.md`

## 依存関係

- `T20260412190917`

## 背景

規約準拠 Markdown を都度手で書かせるより、テンプレート生成と `updated` 更新をコマンドで吸収した方が継続率が高い。
design.md 上は後段フェーズだが、運用導線としては早めに必要。

## 変更内容

### 1. `mdex new task "<title>"`

- `tasks/pending` を既定出力先にする
- ファイル名は `T<UTC timestamp>.md`
- `docs/convention.md` に沿った task テンプレートを生成する

### 2. `mdex new decision "<title>"`

- `decision` または `decisions` を既定出力先にする
- slug ベースのファイル名を生成する
- decision テンプレートを生成する

### 3. `mdex stamp <node-id or path>`

- `updated` frontmatter を当日値へ更新する
- frontmatter が無い場合も最小構成で追加する

## 対象ファイル

- `C:\Codex\mdex\runtime\cli.py`
- 新規 `C:\Codex\mdex\runtime\scaffold.py`
- `C:\Codex\mdex\docs\convention.md` 参照
- 新規または既存 scaffold 系テスト

## 完了条件

1. `mdex new task "example"` が task テンプレートを生成する
2. `mdex new decision "example"` が decision テンプレートを生成する
3. `mdex stamp <path>` が `updated` を更新する
4. `python -m pytest` の追加した scaffold 系テストが通る

## 備考

- project 名は config から取れれば使い、無ければ `unknown` でよい
- テンプレートは最小構成に留め、過剰自動化はしない

---
## 完了手順（Codex 定型）

### パス情報
- このファイル  : `C:\Codex\infra\tasks\pending\T20260412190921.md`
- 完了後の移動先: `C:\Codex\infra\tasks\done\T20260412190921.md`
- プロジェクト  : `C:\Codex\mdex`

### 環境制約
- `rg`（ripgrep）は権限エラーで使用不可
- ファイル検索 → `Get-ChildItem -Path <絶対パス> -Recurse -Filter <pattern>`
- 文字列検索 → `Select-String -Path <絶対パス> -Pattern <pattern> -Recurse`
- パスは常に絶対パスで指定すること（`C:\Codex\...` 形式）

### 完了時の操作
1. このファイルに Result セクションを追記する
   ```
   ## Result
   
   - 実施内容: （何をしたか）
   - 確認方法: （どう検証したか）
   - 備考: （気づいたことがあれば）
   ```

2. ファイルを done/ に移動する
   ```powershell
   Move-Item 'C:\Codex\infra\tasks\pending\T20260412190921.md' 'C:\Codex\infra\tasks\done\T20260412190921.md'
   ```

3. DB 更新は不要（Claude 側が done/ を確認して取り込む）

## Result

- 実施内容: `runtime/scaffold.py` を追加し、CLI `new task` / `new decision` / `stamp` を実装しました。task は `T<UTC timestamp>.md`、decision は slug 名で生成し、`stamp` は frontmatter `updated` を更新/追加します。
- 確認方法: `python -m pytest -q`、`tests/test_phase_a_cli.py::test_new_task_decision_and_stamp_path` と `test_stamp_node_id_resolution_uses_auto_db` で検証。
- 備考: `task_dir` / `decision_dir` は `.mdex/config.json` 優先で解決します。
