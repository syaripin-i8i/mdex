# Task: [mdex] scan_roots 複数 root を正式サポート

**ID**: T20260419125309
**Project**: mdex
**Priority**: high
**Status**: done
**Created**: 2026-04-19T12:53:09.683733+00:00

## Description

タスクファイル: `C:\Codex\infra\tasks\pending\{ID}.md`
実装先: `C:\Codex\mdex`

## 目的
`control/scan_config.json` の `scan_roots` (複数形) を実装側で正式サポートする。現在は public 設定として存在するが runtime は単数形 `scan_root` しか見ておらず、利用者が `scan_roots` で対象を絞ったつもりで効かないという trust boundary 問題を解消する。

## 対象ファイル
- `runtime/dbresolve.py` (`resolve_scan_roots` 追加、旧 `resolve_scan_root` は互換維持)
- `runtime/builder.py::build_index` (list[Path] を受け取れるように)
- `runtime/scanner.py::list_indexable_files` (複数 root 列挙)
- `runtime/cli.py::_cmd_scan` (config から複数 root を解決、`--root` CLI は単一か repeatable かは Codex 判断で決定)
- `tests/test_phase_a_cli.py` (regression test 追加)
- `README.md` / `docs/design.md` (仕様更新)

## 完了条件
- `scan_roots` が JSON array として使える。要素 1 個のときの挙動は現行と同一
- `scan_root` (単数) は `scan_roots` の alias として後方互換維持。両方あるときは `scan_roots` を優先し deprecation warning
- 複数 root で同一 node_id が衝突するケースは ValueError で fail-closed
- 各 root 配下の containment check は現行どおり維持(symlink skip、repo 外 reject)
- regression test: (a) 2 root で非衝突 scan が成功、(b) node_id 衝突で ValueError、(c) `scan_root` 単数でも動く、(d) `scan_roots` が `--root` CLI より優先か否かの挙動を assert
- `pytest -q` all green
- `python -m py_compile runtime/dbresolve.py runtime/builder.py runtime/scanner.py runtime/cli.py` OK

## 非目標
- CLI `--root` の仕様大改訂(現状どおり単一でも可)

## 着手前
- `C:\Codex\deep-research-report.md` の A の所見を参照
- `codegraph context runtime/builder.py` / `runtime/scanner.py` で影響範囲を確認

## Result

scan_roots 複数 root 正式対応。resolve_scan_roots 追加、build_index が list 受付、node_id 衝突 fail-closed。