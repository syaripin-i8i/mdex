# Task: [mdex] Release workflow 骨格(OFF 状態で追加)

**ID**: T20260419125333
**Project**: mdex
**Priority**: high
**Status**: done
**Created**: 2026-04-19T12:53:33.230528+00:00

## Description

タスクファイル: `C:\Codex\infra\tasks\pending\{ID}.md`
実装先: `C:\Codex\mdex`

## 目的
PyPI 公開に向けた release workflow を**手動 trigger(workflow_dispatch)のみ**で追加し、Trusted Publishing + attestation + twine check + sdist install smoke を骨格として用意する。PyPI 側の trusted publisher 登録はユーザーが後から行うため、workflow は登録完了まで発火しない状態で merge する。

## 対象ファイル
- `.github/workflows/release.yml` (新規)
- `docs/release_process.md` (新規、または README に section)

## 完了条件
- `release.yml`:
  - trigger は `workflow_dispatch` のみ(push tag trigger は含めない。将来 enable する際にコメントで明記)
  - `permissions: contents: read, id-token: write`
  - steps:
    1. checkout (SHA pin)
    2. setup-python 3.12 (SHA pin)
    3. `python -m pip install --upgrade pip build twine`
    4. `python -m build`
    5. `python -m twine check dist/*`
    6. sdist install smoke(`pip install dist/*.tar.gz` → `mdex --help`)
    7. wheel install smoke(`pip install --force-reinstall dist/*.whl` → `mdex --help`)
    8. `pypa/gh-action-pypi-publish@<full-sha>` with attestation enabled(`attestations: true`)
  - workflow 内に「PyPI trusted publisher 登録後に有効化可」のコメント
- `docs/release_process.md` に以下を明記:
  - PyPI アカウント作成 → trusted publisher 登録(project name `mdex-cli`、workflow `release.yml`、environment 任意)
  - 手動 release 手順(`gh workflow run release.yml`)
  - 初回 release 前に `pylock.toml` 更新と `CHANGELOG.md` 編集
- actions_lint / workflow の syntax OK(push 時に発火しないこと確認)

## 非目標
- 実際の PyPI upload
- 自動 release trigger(tag push 等)

## 着手前
- `C:\Codex\deep-research-report.md` の B の所見(後半、release flow 部分)を参照
- 既存 `ci.yml` の SHA pin 書式を踏襲

## Result

release.yml を workflow_dispatch only で追加。twine check + sdist/wheel smoke + Trusted Publishing + attestation。docs/release_process.md 追加。