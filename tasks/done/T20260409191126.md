# Task: README の導線を本流優先に再整理し、enrich/scan のレイヤー差を明記する

**ID**: T20260409191126
**Project**: mdex
**Priority**: normal
**Status**: done
**Created**: 2026-04-09T19:11:26+09:00

## Description

実装先: C:\Codex\mdex\README.md
タスクファイル置き場: C:\Codex\infra\tasks\done\

## 目的

README の導線で experimental の `context` が主役に見える点を下げ、実態に合わせて本流を `scan -> find/first/related/query` に寄せる。
また、`enrich` が `node_overrides` へ即時反映される設計を明記し、`enrich` の反映に再 `scan` が必須だと誤読されないようにする。

## 対象ファイル

- `C:\Codex\mdex\README.md`

## 完了条件

- README 冒頭導線を本流優先に変更する
- `context` を補助（experimental）として位置づける
- 作業サイクルを `seed 更新 (scan)` と `write-back 更新 (enrich)` に分けて記述する
- README 関連テストと全体テストが通る

## Result

- 実施内容: `README.md` の「エージェントの使い方」を再構成し、`scan -> find/first/related/query` を本流として先頭に配置、`context` を補助（experimental）へ移動。`enrich` は `node_overrides` への即時反映で再 `scan` 不要（Markdown 本文更新時のみ `scan`）を明記。さらに「作業サイクル」を `seed 更新` と `write-back 更新` の2レイヤー表現へ更新。
- 確認方法: `python -m pytest -q tests/test_readme_examples.py`（2 passed）、`python -m pytest -q`（45 passed）
- 備考: 既存の `quality_repo` 最小メトリクス節・再現サンプルとの整合を維持。
