# Task: [mdex] context/search の file read スキップ + estimated_tokens を DB 化

**ID**: T20260419125356
**Project**: mdex
**Priority**: normal
**Status**: done
**Created**: 2026-04-19T12:53:56.602299+00:00

## Description

タスクファイル: `C:\Codex\infra\tasks\pending\{ID}.md`
実装先: `C:\Codex\mdex`

## 目的
`context` / `search` が全ノード読込 + 候補ごとの file read でスケールしない問題を解消する。scan 時に `estimated_tokens` を DB へ保存し、`context` では再読込しない。

## 対象ファイル
- `runtime/indexer.py` / `runtime/builder.py` (scan 時に estimated_tokens 計算 → DB 保存)
- `runtime/store.py` (`nodes` テーブル schema migration + 読込 API 更新)
- `runtime/context.py::select_context` (`_estimated_tokens_for_node` の file read を廃止、DB 値を使う)
- `runtime/context.py` (`--include-content=False` 時に file read を完全に回避)
- `tests/test_context.py` / `tests/test_store.py` (test 更新)

## 完了条件
- scan 時に各 node の `estimated_tokens` を DB `nodes` テーブルの新 column に保存
- `context` の候補評価で file read をしない(`include_content=True` 指定時のみ最後に content 取得)
- `search_nodes` の重複 `list_nodes` 呼び出しを整理(最低でも 2 回 → 1 回に)
- migration: 旧 DB は自動で column 追加 or scan やり直しで対応(どちらか明記)
- regression test: (a) `context` が include_content=False で file read 0 回(monkeypatch で `Path.read_text` を count)、(b) estimated_tokens が node に紐付けされている、(c) 既存の context test が通る
- `pytest -q` all green

## 非目標
- 全件 Python scoring を DB SQL に寄せる(prefilter 未対応で OK、次段)
- FTS5 / インデックス導入

## 着手前
- `C:\Codex\deep-research-report.md` の D の所見を参照
- `codegraph context runtime/context.py` で呼び出し関係確認

## Result

scan 時に estimated_tokens を DB 保存、context の file read を削減。