# Task: [mdex] scan の per-file 例外隔離 + --strict flag + warnings

**ID**: T20260419125344
**Project**: mdex
**Priority**: normal
**Status**: done
**Created**: 2026-04-19T12:53:44.735970+00:00

## Description

タスクファイル: `C:\Codex\infra\tasks\pending\{ID}.md`
実装先: `C:\Codex\mdex`

## 目的
1 つの不正 UTF-8 / YAML / JSON ファイルで `scan` 全体が failure に倒れる現状を修正する。デフォルトは warn-continue(対象ファイルを skip して warnings に記録)、`--strict` flag 付与時は fail-fast。

## 対象ファイル
- `runtime/builder.py::build_index` (per-file try/except + warnings 収集)
- `runtime/cli.py::_cmd_scan` (`--strict` flag 追加)
- `runtime/indexer.py` または `write_sqlite` 周辺 (warnings を出力 JSON に含める)
- `schemas/scan.schema.json` (`warnings` フィールド追加)
- `docs/schema_versioning.md` (変更内容を minor bump として記録)
- `CHANGELOG.md` (Unreleased 節に追記)
- `tests/` (malformed 入力 fixture + 挙動 test)

## 完了条件
- default 挙動: 1 ファイル壊れていても残りは index 化、出力 JSON `warnings` に `[{path, error}]` 形式で記録
- `--strict` 付与時: 例外を up propagate して exit code 非 0
- scan 出力 schema に `warnings: {type: array, items: {type: object, properties: {path, error}}}` 追加
- regression test: (a) malformed UTF-8 JSON + 正常 .md を default scan で処理、warnings に 1 件、index には良 .md が入る、(b) `--strict` 時に exit code 非 0、(c) 既存の正常系 test が通る
- `pytest -q` all green

## 非目標
- 部分的 parse recovery(bad JSON の partial fields 拾う等)
- warnings の永続化(DB 保存)

## 着手前
- `C:\Codex\deep-research-report.md` の C の所見を参照
- 既存 `schemas/scan.schema.json` の構造を Read

## Result

scan の per-file 隔離 + --strict + warnings 契約化。schema/scan.schema.json 更新。