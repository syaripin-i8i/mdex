# Task: mdex 構造改善（ImportError整理 / indexer責務純化 / related精度向上）

**ID**: T20260409132703
**Project**: mdex
**Priority**: high
**Status**: pending
**Created**: 2026-04-09T13:27:03+09:00

## Description

以下3点を実装する。

1. 	ry/except ImportError の多重 import fallback を除去し、パッケージ実行前提の import に統一
2. indexer.py の薄い uild_index 委譲を廃止し、責務を「書き出し専用」に純化
3. esolver.py の elated を強化（タグ一致・type一致加点、未接続だが同タグノードの候補化）

## 完了条件

- runtime モジュールの import 方式が統一され、fallback が消えている
- indexer は write 層として明確化されている
- related が edge 以外のシグナル（tag/type）でも候補を返せる
- pytest が pass する

---
## 完了手順（Codex 定型）

### パス情報
- このファイル  : $path
- 完了後の移動先: C:\Codex\infra\tasks\done\T20260409132703.md
- プロジェクト  : C:\Codex\mdex

### 完了時の操作
1. このファイルに Result セクションを追記する
2. ファイルを done/ に移動する
3. DB 更新は不要（done/ を取り込み）

## Result

- 実施内容: C:\Codex\mdex\runtime\builder.py, untime\cli.py, untime\resolver.py から 	ry/except ImportError fallback を除去し、untime.* のパッケージ import に統一。
- 実施内容: C:\Codex\mdex\runtime\indexer.py の uild_index 委譲を廃止し、責務を write_json / write_sqlite の書き出し専用に純化。uild_index 呼び出しは cli.py で untime.builder から直接実行する形へ変更。
- 実施内容: C:\Codex\mdex\runtime\resolver.py の elated_nodes() を強化。resolved edge 重み付けに加えて shared_tags と same_type を加点対象にし、未接続でも同タグ/同型ノードが候補化されるよう実装。
- 実施内容: C:\Codex\mdex\tests\test_mdex.py を更新し、CLI subprocess 呼び出しを python -m runtime.cli に切り替え。elated の tag/type シグナルを検証するテストを追加。	ests\fixtures\repo\docs\tag_peer.md / 	ype_peer.md を追加。
- 確認方法: python -m py_compile runtime/__init__.py runtime/scanner.py runtime/parser.py runtime/builder.py runtime/indexer.py runtime/store.py runtime/resolver.py runtime/reader.py runtime/cli.py を実行して成功。
- 確認方法: python -m pytest -q を実行し 8 passed を確認。
- 確認方法: python -m runtime.cli scan/list/query/related を実行し動作確認。
