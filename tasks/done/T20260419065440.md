# Task: mdex に GitHub Actions CI を導入（pytest matrix / pip-audit / dependency-review）

**ID**: T20260419065440
**Project**: mdex
**Priority**: normal
**Status**: done
**Created**: 2026-04-19T06:54:40.390169+00:00

## Description

タスクファイル: C:\Codex\infra\tasks\pending\{ID}.md
実装先: C:\Codex\mdex

## 背景
公開前調査で CI/CD と継続的セキュリティ監査が欠けていることが指摘された。最低限の 4 本を揃える。

## 対象
- .github/workflows/ci.yml を新規作成。以下 3 ジョブを持つこと:
  1. **test**: Python 3.10 / 3.11 / 3.12 の matrix で `pip install -e .[dev]` → `python -m pytest -q`
  2. **audit**: `pip-audit` を Python 3.12 で走らせる
  3. **dependency-review**: pull_request 時のみ `actions/dependency-review-action@v4`
- 既存 pyproject.toml の `[project.optional-dependencies] dev` に pytest 以外の必要ツール（`pip-audit`）を追加するか、audit ジョブで別途インストールする
- README の開発セクションに CI バッジを追加
- scripts/ 配下に必要なら local 再現用の簡易スクリプトを追加（任意）

## 完了条件
- PR を作ると上記 3 ジョブが起動し、現行コードで test / audit が green
- dependency-review は PR のみで起動、main への直 push では起動しない
- README に CI バッジがある
- ワークフローは `permissions:` を最小化（read-only をデフォルト）

## 非目標
- release ワークフロー（別タスク）
- signing / SBOM 生成（別タスク）
- self-hosted runner 導入

## Result

.github/workflows/ci.yml に test(3.10/3.11/3.12)/audit(pip-audit)/dependency-review(PR限定) 3ジョブ、README に CI バッジ、pip-audit を dev 依存追加。