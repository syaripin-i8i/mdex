# Task: 品質固定-T3: first に distance / reason を追加

**ID**: T20260409054358
**Project**: mdex
**Priority**: normal
**Status**: done
**Created**: 2026-04-09T05:43:58.941070+00:00

## Description

タスクファイル: C:\Codex\infra\tasks\pending\
実装先: C:\Codex\mdex

## 目的
first コマンドの出力に順序の根拠を付与する。
順位だけあると後で調整不能になる。distance があれば誤動作の検証がしやすい。

## 変更内容

### runtime/resolver.py
prerequisite_order() の返却 payload に以下を追加:
- distance: int（直接 depends_on なら 1、その先なら 2、3...）
- reason: str（"direct depends_on" / "transitive depends_on (depth N)"）

### 出力例
```
{
  "node": {...},
  "prerequisites": [
    { "id": "spec/b.md", "order": 1, "distance": 2, "reason": "transitive depends_on (depth 2)" },
    { "id": "decision/a.md", "order": 2, "distance": 1, "reason": "direct depends_on" }
  ]
}
```

### tests/test_resolver.py
- distance が正しく付与されること
- 直接依存は distance=1、間接依存は distance=2 以上であること

## 完了条件
- first コマンドの出力に distance と reason が含まれる
- python -m pytest -q が通る
