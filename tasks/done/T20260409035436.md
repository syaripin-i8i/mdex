# Task: mdex trial: doneタスク全量で関連付け精度を検証し、参照抽出を改善

**ID**: T20260409035436
**Project**: mdex
**Priority**: high
**Status**: pending
**Created**: 2026-04-09T03:54:36+09:00

## Description

**実装先**: `C:\Codex\mdex\runtime\parser.py`, `C:\Codex\mdex\runtime\builder.py`
**目的**: `C:\Codex\infra\tasks\done` を対象に、タスク間の関連（参照ID）を実用的に引けるか検証し、必要な抽出ロジックを補強する。

## 実装対象

- done 全量スキャン（ベースライン計測）
- task markdown 書式（`**Project**`, `**Status**` など）の fallback 抽出
- `TYYYYMMDDHHMMSS` 形式ID参照を `relates_to` としてエッジ化
- inline/fenced code 由来のノイズリンクを低減

## 完了条件

- `python runtime/cli.py scan --root C:/Codex/infra/tasks/done` で有効な JSON/DB が出力される
- done 全量に対して、タスク間参照エッジがベースラインより増加する
- `--project mdex` で mdex タスクのみ抽出できる

---
## 完了手順（Codex 定型）

### パス情報
- このファイル  : `C:\Codex\infra\tasks\pending\T20260409035436.md`
- 完了後の移動先: `C:\Codex\infra\tasks\done\T20260409035436.md`
- プロジェクト  : `C:\Codex\mdex`

### 完了時の操作
1. このファイルに Result セクションを追記する
2. ファイルを done/ に移動する
3. DB 更新は不要（done/ を取り込み）

## Result

- 実施内容: `C:\Codex\mdex\runtime\parser.py` を拡張し、frontmatter 非採用の task 形式から `Project/Status/ID` 等を fallback 抽出、`T\d{14}` 参照抽出（`task_refs`）を追加。さらに inline/fenced code をリンク抽出対象から除外し、テンプレ例由来のノイズリンクを抑制。
- 実施内容: `C:\Codex\mdex\runtime\builder.py` を拡張し、`title` 先頭 `Task:` の type 推定、`tasks/done` 配下の status 正規化（`pending/unknown -> done`）、`task_refs` を `relates_to` エッジへ反映する処理を追加。
- 確認方法: 改修前スキャン（`scan --root C:\Codex\infra\tasks\done`）で `180 nodes / 8 edges` を確認。改修後に同条件で再実行し `180 nodes / 59 edges` へ改善、うち内部解決エッジ `47` を確認。`python runtime/cli.py list --index mdex_done_index.json --project mdex` で mdex タスク6件抽出を確認。
- 確認方法: `python runtime/cli.py query --index mdex_done_index.json --node T20260330022925.md --depth 1` で `relates_to` による隣接タスク解決（2件）を確認。
- 備考: 出力ファイルは `C:\Codex\mdex\mdex_done_index.json` と `C:\Codex\mdex\mdex_done_index.db`。
