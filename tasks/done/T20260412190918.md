# Task: mdex Phase A 入口 - `start` コマンドと action-oriented `context`

**ID**: T20260412190918
**Project**: mdex
**Priority**: high
**Status**: pending
**Created**: 2026-04-12T19:09:18+00:00

## Description

**タスクファイル**: `C:\Codex\infra\tasks\pending\T20260412190918.md`
**実装コミット先**: `C:\Codex\mdex`

## 参照設計

- `C:\Codex\mdex\docs\phase_a_agent_flow.md`

## 依存関係

- `T20260412190917`

## 背景

現状の `context` は候補集合としては有用だが、AI がそのまま作業開始に使うには行動情報が足りない。
Phase A では `mdex start "<task>"` を導入し、読む順序と次アクションを返す開始 API にする。

## 変更内容

### 1. `context` の action-oriented 拡張

既存出力を残したまま、`--actionable` フラグで以下を追加する。

- `recommended_read_order`
- `recommended_next_actions`
- `deferred_nodes`
- `confidence`
- `why_this_set`

### 2. `mdex start` の追加

新コマンド:

```bash
mdex start "<task>" [--budget <n>] [--limit <n>] [--include-content]
```

内部で以下を統合する。

- DB 自動解決
- index 存在確認
- `context --actionable`
- 必要に応じた `first` / `related` の軽い統合
- 次アクションの提案

### 3. JSON 契約の整備

単なる検索結果ではなく、以下を返すこと。

- `task`
- `db`
- `index_status`
- `recommended_read_order`
- `recommended_next_actions`
- `deferred_nodes`
- `confidence`
- `why_this_set`

## 対象ファイル

- `C:\Codex\mdex\runtime\cli.py`
- `C:\Codex\mdex\runtime\context.py`
- `C:\Codex\mdex\runtime\resolver.py`
- 新規 `C:\Codex\mdex\runtime\start.py`
- `C:\Codex\mdex\tests\test_context.py`
- `C:\Codex\mdex\tests\test_cli_e2e.py`

## 完了条件

1. `mdex context "root decision" --actionable` が追加フィールドを含む JSON を返す
2. 既存 `mdex context "..."` の `nodes` 形式が維持される
3. `mdex start "root decision"` が valid JSON を返す
4. `quality_repo` で `recommended_read_order` が空でない
5. `recommended_next_actions` が 1 件以上返る
6. `python -m pytest tests/test_context.py tests/test_cli_e2e.py -q` が通る

## 備考

- 既存スコアリングの大改造はしない
- `start` は Phase A の主入口なので、出力の説明責任を重視する

---
## 完了手順（Codex 定型）

### パス情報
- このファイル  : `C:\Codex\infra\tasks\pending\T20260412190918.md`
- 完了後の移動先: `C:\Codex\infra\tasks\done\T20260412190918.md`
- プロジェクト  : `C:\Codex\mdex`

### 環境制約
- `rg`（ripgrep）は権限エラーで使用不可
- ファイル検索 → `Get-ChildItem -Path <絶対パス> -Recurse -Filter <pattern>`
- 文字列検索 → `Select-String -Path <絶対パス> -Pattern <pattern> -Recurse`
- パスは常に絶対パスで指定すること（`C:\Codex\...` 形式）

### 完了時の操作
1. このファイルに Result セクションを追記する
   ```
   ## Result
   
   - 実施内容: （何をしたか）
   - 確認方法: （どう検証したか）
   - 備考: （気づいたことがあれば）
   ```

2. ファイルを done/ に移動する
   ```powershell
   Move-Item 'C:\Codex\infra\tasks\pending\T20260412190918.md' 'C:\Codex\infra\tasks\done\T20260412190918.md'
   ```

3. DB 更新は不要（Claude 側が done/ を確認して取り込む）

## Result

- 実施内容: `runtime/context.py` に action-oriented 拡張（`recommended_read_order` / `recommended_next_actions` / `deferred_nodes` / `confidence` / `why_this_set`）を追加し、`runtime/start.py` + CLI `start` コマンドを実装しました。
- 確認方法: `python -m pytest -q`、`tests/test_phase_a_cli.py` で `context --actionable` と `start` の JSON 契約・非空配列を検証。
- 備考: 既存 `context` の `nodes` 形式は維持し、`--actionable` 指定時のみ拡張フィールドを返す構成です。
