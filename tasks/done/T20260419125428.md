# Task: [mdex] coverage gate + secret scan + SAST + CI smoke 拡充

**ID**: T20260419125428
**Project**: mdex
**Priority**: normal
**Status**: done
**Created**: 2026-04-19T12:54:28.056276+00:00

## Description

タスクファイル: `C:\Codex\infra\tasks\pending\{ID}.md`
実装先: `C:\Codex\mdex`

## 目的
現行 CI は「広く試す」方向は強いが、リリース品質の自動下限が弱い(coverage gate / secret scan / SAST / twine check / sdist smoke 無し)。公開向けに段差を追加する。

## 対象ファイル
- `pyproject.toml` (`[project.optional-dependencies].dev` に `pytest-cov` 追加)
- `.github/workflows/ci.yml` (coverage job / gitleaks / codeql / twine check / sdist smoke 追加)
- `tests/fixtures/` (malformed UTF-8 / bad YAML / broken JSON fixture)
- `.github/workflows/codeql.yml` (新規、CodeQL 用)

## 完了条件
- `pytest-cov` 追加、CI の `pytest` step を `pytest -q --cov=runtime --cov-report=term --cov-fail-under=80` に更新(threshold は Codex が現状の coverage を測って妥当な数値で設定、80 は目安)
- `gitleaks` action をSHA pin で追加(push/pr 両方で発火)
- CodeQL workflow を追加(Python、weekly schedule + push)
- `build-smoke` job に `python -m twine check dist/*` と sdist install smoke(`pip install dist/*.tar.gz` → `mdex --help`)を追加
- malformed input fixture を `tests/fixtures/` に追加(T4 の per-file 隔離 test で使用可能)
- `python -m pytest -q` ローカル green
- CI 全 green(新規 job 含む)

## 非目標
- semgrep / trufflehog 等の追加 scanner
- performance regression test
- coverage threshold の段階的引き上げ(初回は現状 baseline で固定)

## 着手前
- `C:\Codex\deep-research-report.md` の G の所見を参照
- T4 と fixture を共有するため、T4 が先 or 同時 merge 想定
- CodeQL は https://github.com/github/codeql-action の最新 v3 SHA を使う

## Result

pytest-cov + coverage gate(40%) + gitleaks + CodeQL + twine check + sdist smoke 追加。CI 段差強化。