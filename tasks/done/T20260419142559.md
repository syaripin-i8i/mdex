# Task: [mdex/0.1.1] start と context --actionable の使い分け判断表を明示

**ID**: T20260419142559
**Project**: mdex
**Priority**: low
**Status**: done
**Created**: 2026-04-19T14:25:59.830758+00:00

## Description

タスクファイル: C:\Codex\infra\tasks\pending\{ID}.md
実装先: C:\Codex\mdex\AGENT.md および C:\Codex\mdex\README.md (Command Selection Rules 表)

## 目的

AI エージェントが `mdex start` と `mdex context --actionable` のどちらを先に打つべきか、初見で迷わず決められる判断ロジックを明文化する。

## 背景

実運用で Claude が使った感触として、「start を打つべきか、直接 context --actionable でいきなり actionable に取るべきか」の分岐で一瞬迷う。README の Command Selection Rules 表は各コマンドの単独説明はあるが、**併用順序**の推奨が薄い。

## 完了条件

1. AGENT.md に decision tree または判断表を追加:
   - 「タスクの入口が既に特定ファイル候補に絞られているか」などの判断軸
   - start → context の順序、context だけで済むケース、start だけで済むケースを明示
2. README の Command Selection Rules 表に「start → context の典型シーケンス」を 1 行で追加、詳細は AGENT.md に委譲
3. 既存の Protocol 表との整合性を保つ（protocol は標準フロー、判断表は分岐の微調整）

## 非ゴール

- start / context の動作仕様変更
- 新コマンド追加

## 参考

- 実運用フィードバック: Claude が「context だけでよかったのに start を先に打って二重に読み込みそうになる」場面を経験

---
## 完了手順（Codex 定型）

### パス情報
- このファイル  : `C:\Codex\infra\tasks\pending\T20260419142559.md`
- 完了後の移動先: `C:\Codex\infra\tasks\done\T20260419142559.md`
- プロジェクト  : `C:\Codex\mdex`

### 環境制約
- `rg`（ripgrep）は権限エラーで使用不可
- ファイル検索 → `Get-ChildItem -Path <絶対パス> -Recurse -Filter <pattern>`
- 文字列検索 → `Select-String -Path <絶対パス> -Pattern <pattern> -Recurse`
- パスは常に絶対パスで指定すること（`C:\Codex\...` 形式）

### 完了時の操作
1. このファイルに Result セクションを追記する
   ```
   ## Result
   
   - 実施内容: （何をしたか）
   - 確認方法: （どう検証したか）
   - 備考: （気づいたことがあれば）
   ```

2. ファイルを done/ に移動する
   ```powershell
   Move-Item 'C:\Codex\infra\tasks\pending\T20260419142559.md' 'C:\Codex\infra\tasks\done\T20260419142559.md'
   ```

3. DB 更新は不要（Claude 側が done/ を確認して取り込む）

## Result

- 実施内容:
  - `AGENT.md` に `start` vs `context --actionable` の判断表を追加
  - 判断軸（入口未確定/既知、changed files 有無、終了前確認）を明示
  - `README.md` の `Command Selection Rules` に典型シーケンス `start -> context --actionable` を 1 行追加し、詳細を `AGENT.md` へ委譲
  - Protocol（標準手順）との整合を維持
- 確認方法:
  - `python -m pytest -q tests/test_readme_examples.py tests/test_docs_consistency.py`
  - 目視で `README.md` / `AGENT.md` の判断記述が重複せず参照関係になっていることを確認
- 備考:
  - 「context だけで済むケース」と「start 先行ケース」を明示して初回迷いを減らした

### Learning Note
- symptom: 実運用で start と context の先行順に迷う
- wrong_first_guess: 既存の command table だけで十分に判断できると思った
- actual_cause: コマンド単体説明はあっても、入口条件ベースの分岐ロジックが不足していた
- files_that_mattered: C:\Codex\mdex\AGENT.md, C:\Codex\mdex\README.md
- proof_command: `python -m pytest -q tests/test_docs_consistency.py`
- next_time_query_seed: start context actionable decision tree, agent command sequencing, mdex entry heuristic
