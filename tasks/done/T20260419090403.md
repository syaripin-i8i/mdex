# Task: [mdex] config 由来 path を repo 内に閉じ込める

**ID**: T20260419090403
**Project**: mdex
**Priority**: high
**Status**: done
**Created**: 2026-04-19T09:04:03.977580+00:00

## Description

タスクファイル: `C:\Codex\infra\tasks\pending\{ID}.md`
実装先: `C:\Codex\mdex`

## 目的
`.mdex/config.json` 由来の path(`scan_root` / `scan_config` / `task_dir` / `decision_dir` / `db` 等)が repo 外を指すことを拒否し、public CLI としての信頼境界を確保する。explicit な CLI 引数 `--db` だけは例外として残す。

## 対象ファイル
- `runtime/dbresolve.py`
- `runtime/scaffold.py`(task_dir / decision_dir 利用箇所)
- `tests/test_phase_a_cli.py`(regression test 追加)

## 完了条件
- `_ensure_within_repo` 相当の helper を追加し、config 由来 path に必ず containment check を通す
- absolute path / `..` / symlink 経由で repo 外を指す config 値を ValueError で reject する
- regression test: `task_dir = "../outside"` や `scan_root = "C:\\tmp"` を reject することを assert
- `python -m py_compile runtime/dbresolve.py runtime/scaffold.py` OK
- `pytest -q` all green

## 非目標
- explicit CLI 引数 `--db` の挙動変更(これは引き続き absolute path 可)
- scanner 側の symlink 対応(別タスク)

## 着手前
- `codegraph context runtime/dbresolve.py` で構造確認
- `C:\Codex\deep-research-report.md` の「信頼境界を repo 内に固定する」節を参照

## Result

dbresolve/scaffold に repo containment 強制。regression test 追加。commit ee3bbc3 に含む。