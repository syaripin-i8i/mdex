# Task: mdex open/stamp の path containment 強制（任意ファイル読取・改変を封じる）

**ID**: T20260419065413
**Project**: mdex
**Priority**: high
**Status**: done
**Created**: 2026-04-19T06:54:13.187467+00:00

## Description

タスクファイル: C:\Codex\infra\tasks\pending\{ID}.md
実装先: C:\Codex\mdex

## 背景
公開前調査で、`mdex open` と `mdex stamp` が「インデックス済みノードかどうか」を検証せず、絶対パスや `..` を含む経路を受理していることが判明。AI エージェント経由で使う際に任意ファイル読取・任意ファイル更新に近いリスクがある。公開前の必須対応。

## 対象ファイル（着手前に codegraph context で依存関係を確認すること）
- runtime/reader.py（`_candidate_paths` / `read_node_text`）
- runtime/scaffold.py（`_resolve_stamp_path` / stamp 実体）
- runtime/cli.py（`_cmd_open` / `_cmd_stamp` の入力ハンドリング）
- tests/ 配下（新規テスト追加）

## 要件
1. `open` の受理対象を「index DB に登録済みの node id」に限定する
2. パス解決の最終段で `Path.resolve().relative_to(scan_root)` を満たさない入力を拒否する（containment check）
3. `stamp` も同様に indexed node のみ更新可とし、scan_root 外への書き込みを不可能にする
4. 絶対パス入力・`..` を含む相対パス入力は明示的にエラーで拒否（黙って scan_root に解釈し直さない）
5. CLI エラーメッセージは「なぜ拒否されたか」を agent が判別できる粒度で返す

## 完了条件
- indexed node でない入力、絶対パス、`..` 経由、scan_root 外への resolve 全ての拒否が tests/ で検証されている
- 既存の open / stamp の正常系テストが全通過
- README の該当節（mdex open / mdex stamp）に containment 挙動を追記
- pytest green

## 非目標
- scan 対象・除外規則の変更
- JSON 出力契約の変更
- CLI オプションの破壊的変更（拒否挙動の追加のみ）

## 参考
- 公開前調査報告の「セキュリティ上の脆弱性」節と「改善計画」節を参照

## Result

open/stamp を indexed node 限定＋scan_root containment に変更。reader/scaffold/cli 改修、拒否系テスト追加、69 passed。