# Task: quality_repo の公開前メトリクスを README か docs に固定する

**ID**: T20260409080726
**Project**: mdex
**Priority**: normal
**Status**: pending
**Created**: 2026-04-09T08:07:26+00:00

## Description

実装先: C:\Codex\mdex\README.md または C:\Codex\mdex\docs\
タスクファイル置き場: C:\Codex\infra\tasks\pending\

## 目的

公開前に強く主張したい最小メトリクスを、`quality_repo` ベースで短く書ける形にする。欲しいのは大規模ベンチマークではなく、README や docs に載せられる「この fixture ではこれが保証される」という最小の事実セット。

## 固定したい主張

1. `scan` 後の edge 品質
   - `resolved = 6`
   - `unresolved = 2`
   - `total = 8`
   - `resolution_rate = 75.0`
2. `first` の前提順が安定していること
   - `design/root.md` に対する上位 2 件は `spec/b.md` → `decision/a.md`
3. `enrich` 後に override が保持されること
   - `design/root.md` を `enrich` した後、再度 `scan` しても `summary_source = "agent"` が残る

## 対象ファイル

- `C:\Codex\mdex\README.md` または `C:\Codex\mdex\docs\quality_metrics.md`
- 必要なら `C:\Codex\mdex\tests\test_mdex.py`
- 既存の `C:\Codex\mdex\tests\test_resolver.py`
- 既存の `C:\Codex\mdex\tests\test_enrich.py`

## 完了条件

- 上の 3 主張を README か docs に短く追加する
- 既存テストで既に担保できているものは再利用し、重複した重いテストは増やしすぎない
- もし `scan` の exact metric を機械検証していないなら、その分だけ最小追加する
- どのコマンドと fixture を前提にした数値なのかが文書から分かるようにする
- `python -m pytest -q` が通ること

## 備考

- README が長くなりすぎるなら `docs/quality_metrics.md` を新規追加し、README からリンクしてもよい
- 「公開前の主張」を増やしすぎないこと。今回は `quality_repo` の最小 3 点だけで十分

---
## 完了手順（Codex 定型）

### パス情報
- このファイル  : `C:\Codex\infra\tasks\pending\T20260409080726.md`
- 完了後の移動先: `C:\Codex\infra\tasks\done\T20260409080726.md`
- プロジェクト  : `C:\Codex\mdex`

### 環境制約
- `rg`（ripgrep）は権限エラーで使用不可
- ファイル検索 → `Get-ChildItem -Path <絶対パス> -Recurse -Filter <pattern>`
- 文字列検索 → `Select-String -Path <絶対パス> -Pattern <pattern> -Recurse`
- パスは常に絶対パスで指定すること（`C:\Codex\...` 形式）

### 完了時の操作
1. このファイルに Result セクションを追記する
   ```
   ## Result
   
   - 実施内容: （何をしたか）
   - 確認方法: （どう検証したか）
   - 備考: （気づいたことがあれば）
   ```

2. ファイルを done/ に移動する
   ```powershell
   Move-Item 'C:\Codex\infra\tasks\pending\T20260409080726.md' 'C:\Codex\infra\tasks\done\T20260409080726.md'
   ```

3. DB 更新は不要（Claude 側が done/ を確認して取り込む）
## Result

- 実施内容: `README.md` に `quality_repo` の「公開前の最小メトリクス」節を追加し、(1) `scan` の `8/6/2` と `resolution_rate=75.0`、(2) `first` の順序 `spec/b.md -> decision/a.md`、(3) `enrich` 後の再 `scan` でも `summary_source=agent` が保持される、の 3 主張を明記しました。あわせて `tests/test_cli_e2e.py` に `resolution_rate == 75.0` の固定を追加し、`tests/test_readme_examples.py` に README 記述のドリフト検知を追加しました。
- 確認方法: `python -m pytest -q tests/test_cli_e2e.py tests/test_readme_examples.py tests/test_enrich.py::test_enrich_persists_across_scan_via_node_overrides` と `python -m pytest -q`
- 備考: 3 点目の永続性は既存の `test_enrich_persists_across_scan_via_node_overrides` を再利用し、重い重複テストは増やしていません。
