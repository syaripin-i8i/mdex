# Task: Phase2-A1: find コマンド実装

**ID**: T20260409043442
**Project**: mdex
**Priority**: high
**Status**: done
**Created**: 2026-04-09T04:34:42.767300+00:00

## Description

タスクファイル: C:\Codex\infra\tasks\pending\
実装先: C:\Codex\mdex

## 目的
`mdex find <query>` コマンドを追加する。
title / summary / tags をキーワード検索し、マッチしたノード一覧を返す探索入口コマンド。

## 対象ファイル
- runtime/store.py に `search_nodes(db_path, query)` を追加
  - LIKE 検索（title, summary, tags_json を対象）
  - SQL は store.py に閉じ、CLI から直叩きしない
  - 将来の FTS5 差し替えを考慮して関数境界を明確に切ること
- runtime/cli.py に `_cmd_find` と subparser `find` を追加
  - 引数: `query`（位置引数）, `--db`, `--limit`（デフォルト20）
  - 出力: `id\ttitle\ttype\tstatus` のタブ区切り（list と同形式）
- tests/test_mdex.py に検証を追加

## 完了条件
- `mdex find <keyword>` で title/summary/tags マッチノードが返る
- マッチなしは空出力（エラーにしない）
- `python -m pytest -q` が通る
