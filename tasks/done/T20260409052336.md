# Task: AI-first リファクタ2: context --include-content / scan JSON出力 / --index 除去 / enrich --path

**ID**: T20260409052336
**Project**: mdex
**Priority**: high
**Status**: pending
**Created**: 2026-04-09T05:23:36.689190+00:00

## Description

タスクファイル: C:\Codex\infra\tasks\pending\
実装先: C:\Codex\mdex

## 前提
mdex は AI エージェント専用ツール。人間向け設計を排除し、エージェントの呼び出しコストを最小化する。
AI-first リファクタ1（T20260409052031）完了後に着手すること。

## 変更内容

### 1. context コマンドに --include-content フラグを追加
- デフォルト: node ID・priority・score・estimated_tokens のみ返す（軽量）
- --include-content 指定時: content フィールドにファイル本文を含めて返す
- これにより context → open の N+1 呼び出しを1回に削減できる
- reader.read_node_text() を使って本文を取得する

### 2. scan コマンドの出力を JSON 化
- 現状: stderr にテキスト "[mdex] N nodes, M edges -> ..." を出力
- 変更後: stdout に JSON サマリを出力
  {"nodes": N, "edges": {"total": M, "resolved": R, "unresolved": U, "resolution_rate": 0.0}, "output": {"json": "path", "db": "path"}}
- stderr には何も出さない（エラー時のみ JSON エラーを stderr）

### 3. --index フォールバックオプションを全コマンドから除去
- list / query / open / related の --index オプションを削除する
- JSON フォールバック経路のコードも合わせて削除
- SQLite のみをサポートする。これは退行ではなく AI 向け簡素化
- テストから --index を使っているケースも削除

### 4. enrich コマンドに --path オプションを追加（Phase3-B と協調）
- --path <絶対パス> を受け取り、db の scan_root から node-id を逆引きして enrich する
- Codex は open したファイルの絶対パスを知っているケースが多いため摩擦を減らす
- node-id が特定できない場合は {"error": "node not found", "path": "..."} を返す
- Phase3-B（T20260409051722）実装前にインターフェースだけ定義しておく

## 対象ファイル
- runtime/cli.py: 上記4点すべて
- runtime/context.py: --include-content 対応（Phase3-A と協調）
- tests/test_mdex.py: --index 除去・scan 出力形式変更に伴うテスト修正

## 完了条件
- mdex context --include-content でファイル本文が JSON に含まれる
- mdex scan の stdout が JSON
- list / query / open / related から --index オプションが消えている
- mdex enrich --path <絶対パス> で node-id を逆引きして動作する
- python -m pytest -q が通る


---
## 完了手順（Codex 定型）

### パス情報
- このファイル  : `C:\Codex\infra\tasks\pending\T20260409052336.md`
- 完了後の移動先: `C:\Codex\infra\tasks\done\T20260409052336.md`
- プロジェクト  : `C:\Codex\mdex`

### 環境制約
- `rg`（ripgrep）は権限エラーで使用不可
- ファイル検索 → `Get-ChildItem -Path <絶対パス> -Recurse -Filter <pattern>`
- 文字列検索 → `Select-String -Path <絶対パス> -Pattern <pattern> -Recurse`
- パスは常に絶対パスで指定すること（`C:\Codex\...` 形式）

### 完了時の操作
1. このファイルに Result セクションを追記する
   ```
   ## Result
   
   - 実施内容: （何をしたか）
   - 確認方法: （どう検証したか）
   - 備考: （気づいたことがあれば）
   ```

2. ファイルを done/ に移動する
   ```powershell
   Move-Item 'C:\Codex\infra\tasks\pending\T20260409052336.md' 'C:\Codex\infra\tasks\done\T20260409052336.md'
   ```

3. DB 更新は不要（Claude 側が done/ を確認して取り込む）
## Result

- 実施内容: C:\Codex\mdex\runtime\cli.py を改修し、context コマンドに --include-content を追加。デフォルトは軽量ノード情報のみ、--include-content 指定時に本文を content として返すよう対応。
- 実施内容: scan の成功時出力を stderr テキストから stdout JSON に変更し、
odes/dges/output を返す形式へ統一。
- 実施内容: list / query / open / elated から --index フォールバック経路を削除し、SQLite のみサポートに簡素化。
- 実施内容: nrich に --path <絶対パス> を追加し、DB の scan_root から node-id を逆引きして処理できるよう実装。
- 実施内容: untime/context.py・untime/tokens.py・untime/enrich.py・untime/store.py を追加/拡張し、上記機能の実装境界をモジュールへ分離。
- 実施内容: C:\Codex\mdex\tests\test_mdex.py を更新し、scan JSON出力、context include-content、--index除去、enrich --path の検証を追加。
- 確認方法: python -m py_compile（対象runtimeファイル）成功、python -m pytest -q で 17 passed を確認。
- 備考: 成功時は stdout JSON、エラー時は stderr JSON を返すAI-first出力仕様へ統一。
