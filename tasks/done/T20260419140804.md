# Task: [mdex/0.1.1] SAST 強化(Semgrep 追加 or CodeQL custom query)

**ID**: T20260419140804
**Project**: mdex
**Priority**: low
**Status**: done
**Created**: 2026-04-19T14:08:04.559482+00:00

## Description

タスクファイル: `C:\Codex\infra\tasks\pending\{ID}.md`
実装先: `C:\Codex\mdex`

## 背景
0.1.0 では CodeQL default setup に寄せたが、監査で「SAST がまだ薄い」と指摘。Semgrep や CodeQL custom query 追加で多角化する宿題。

## 目的
SAST のカバレッジを広げる。特に Python 特有のパストラバーサル・YAML/JSON deserialization 周りを押さえる。

## 対象ファイル
- `.github/workflows/` (Semgrep workflow 追加)
- または CodeQL 設定で custom query pack を指定

## 完了条件
- 2 本目の SAST (Semgrep or CodeQL custom) が CI で動く
- false positive は baseline 化
- SECURITY.md に SAST ツール一覧を記載

## 非目標
- 全 OWASP top 10 カバー(できる範囲で)

## 着手前
- Semgrep の Python ruleset を確認、mdex と相性が良いか見る
- 既存の default CodeQL result を確認(Security tab で alert 数 見る)

---
## 完了手順（Codex 定型）

### パス情報
- このファイル  : `C:\Codex\infra\tasks\pending\T20260419140804.md`
- 完了後の移動先: `C:\Codex\infra\tasks\done\T20260419140804.md`
- プロジェクト  : `C:\Codex\mdex`

### 環境制約
- `rg`（ripgrep）は権限エラーで使用不可
- ファイル検索 → `Get-ChildItem -Path <絶対パス> -Recurse -Filter <pattern>`
- 文字列検索 → `Select-String -Path <絶対パス> -Pattern <pattern> -Recurse`
- パスは常に絶対パスで指定すること（`C:\Codex\...` 形式）

### 完了時の操作
1. このファイルに Result セクションを追記する
   ```
   ## Result
   
   - 実施内容: （何をしたか）
   - 確認方法: （どう検証したか）
   - 備考: （気づいたことがあれば）
   ```

2. ファイルを done/ に移動する
   ```powershell
   Move-Item 'C:\Codex\infra\tasks\pending\T20260419140804.md' 'C:\Codex\infra\tasks\done\T20260419140804.md'
   ```

3. DB 更新は不要（Claude 側が done/ を確認して取り込む）

## Result

- 実施内容:
  - CI に 2 本目 SAST として `semgrep` ジョブを追加
  - カスタムルール `C:\Codex\mdex\.semgrep\mdex-python.yml` を新設
    - YAML unsafe load
    - pickle deserialization
    - path handling hotspot (warning)
  - `SECURITY.md` に自動セキュリティスキャン一覧（Semgrep / dependency review / gitleaks）を追記
- 確認方法:
  - `semgrep --config .semgrep/mdex-python.yml --error --severity ERROR --metrics=off --exclude tests/fixtures`
  - 結果: `0 findings`
- 備考:
  - false positive 低減のため、blocking は ERROR ルールに限定

### Learning Note
- symptom: CodeQL 依存だけでは Python 固有の deserialization/path 監視観点が薄い
- wrong_first_guess: 既存 workflow に軽くルール追加するだけで十分だと思った
- actual_cause: CI で明示的に Semgrep 実行ジョブを持たないと継続的監視にならない
- files_that_mattered: C:\Codex\mdex\.github\workflows\ci.yml, C:\Codex\mdex\.semgrep\mdex-python.yml, C:\Codex\mdex\SECURITY.md
- proof_command: `semgrep --config .semgrep/mdex-python.yml --error --severity ERROR --metrics=off --exclude tests/fixtures`
- next_time_query_seed: semgrep custom rules python, yaml unsafe load rule, pickle deserialization scan, ci sast layering
