# Task: AI-first リファクタ: 全コマンド出力 JSON デフォルト化 + enrich プロンプト修正

**ID**: T20260409052031
**Project**: mdex
**Priority**: high
**Status**: pending
**Created**: 2026-04-09T05:20:31.399411+00:00

## Description

タスクファイル: C:\Codex\infra\tasks\pending\
実装先: C:\Codex\mdex

## 前提・背景
mdex は Claude Code / Codex などの AI エージェントのためのツールであり、人間向けではない。
AGENT.md の Core Principles にこの原則を追記済み。
現在の list / find / orphans はタブ区切りテキストをデフォルト出力しており、AI が使いにくい。修正する。

## 変更内容

### 1. list / find / orphans の出力を JSON デフォルトに変更
- デフォルト出力: JSON 配列
- --format table を指定した場合のみタブ区切りテキストを出力（後方互換として残す）
- list はすでに --format json を持っているので、デフォルトを逆にするだけでよい
- find / orphans も同様に --format table オプションを追加した上でデフォルトを JSON に変更

### 2. エラー出力を JSON 統一
- 全コマンドで stderr に出す [mdex] プレフィックスのテキストエラーを JSON 化する
- 形式: {"error": "node not found", "node_id": "..."} を stderr に出力
- 終了コードは現状維持（0=成功, 2=エラー）

### 3. enrich コマンド（Phase3-B で実装予定）のプロンプト仕様を定義
- runtime/enrich.py にプロンプト定数 ENRICH_PROMPT を定義すること
- 内容: 「別の AI エージェントがタスク着手前にこのファイルを読むべきか判断するための情報を2〜3文で書いてください。何が書かれているか・どんな制約や判断を含むか・いつ参照すべきかを含めること。」
- Phase3-B タスク（T20260409051722）がこの定数を使う前提で先行定義する

## 対象ファイル
- runtime/cli.py: list / find / orphans のデフォルト出力変更、エラー JSON 化
- tests/test_mdex.py: 出力形式変更に伴うテスト修正
- runtime/enrich.py: 新規作成（プロンプト定数の定義のみ。実装本体は Phase3-B）

## 完了条件
- mdex list / find / orphans がデフォルトで JSON 配列を返す
- --format table でタブ区切りが返る
- エラー時に JSON が stderr に出る
- python -m pytest -q が通る


---
## 完了手順（Codex 定型）

### パス情報
- このファイル  : `C:\Codex\infra\tasks\pending\T20260409052031.md`
- 完了後の移動先: `C:\Codex\infra\tasks\done\T20260409052031.md`
- プロジェクト  : `C:\Codex\mdex`

### 環境制約
- `rg`（ripgrep）は権限エラーで使用不可
- ファイル検索 → `Get-ChildItem -Path <絶対パス> -Recurse -Filter <pattern>`
- 文字列検索 → `Select-String -Path <絶対パス> -Pattern <pattern> -Recurse`
- パスは常に絶対パスで指定すること（`C:\Codex\...` 形式）

### 完了時の操作
1. このファイルに Result セクションを追記する
   ```
   ## Result
   
   - 実施内容: （何をしたか）
   - 確認方法: （どう検証したか）
   - 備考: （気づいたことがあれば）
   ```

2. ファイルを done/ に移動する
   ```powershell
   Move-Item 'C:\Codex\infra\tasks\pending\T20260409052031.md' 'C:\Codex\infra\tasks\done\T20260409052031.md'
   ```

3. DB 更新は不要（Claude 側が done/ を確認して取り込む）
## Result

- 実施内容: C:\Codex\mdex\runtime\cli.py を更新し、list / ind / orphans のデフォルト出力を JSON 配列に変更。--format table 指定時のみタブ区切り出力にした。
- 実施内容: ind / orphans に --format {json,table} を追加し、list と同じ出力制御へ統一した。
- 実施内容: CLI のエラー出力を JSON に統一する _emit_error() を追加し、各コマンドのエラー時 stderr を {"error": ..., ...} 形式へ変更（終了コードは 2 のまま維持）。
- 実施内容: C:\Codex\mdex\runtime\enrich.py を新規作成し、Phase3-B 先行定義として ENRICH_PROMPT 定数を追加。
- 実施内容: C:\Codex\mdex\tests\test_mdex.py を更新し、list/find/orphans の JSONデフォルト、--format table 後方互換、JSONエラー出力を検証するテストへ修正・追加。
- 確認方法: python -m py_compile C:\Codex\mdex\runtime\cli.py C:\Codex\mdex\runtime\enrich.py を実行して成功。
- 確認方法: python -m pytest -q を実行し 13 passed を確認。
- 備考: scan の進捗 stderr は情報ログとして既存形式を維持し、エラー経路のみ JSON 化。
