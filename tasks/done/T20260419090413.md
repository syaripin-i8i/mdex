# Task: [mdex] scan の symlink / resolved path 外参照を拒否

**ID**: T20260419090413
**Project**: mdex
**Priority**: high
**Status**: done
**Created**: 2026-04-19T09:04:13.766766+00:00

## Description

タスクファイル: `C:\Codex\infra\tasks\pending\{ID}.md`
実装先: `C:\Codex\mdex`

## 目的
`scan` 時に repo 内 symlink が repo 外を指すケースや、resolve 後に containment が崩れるファイルを index に取り込まないようにする。`_node_id_for_path` の absolute-path fallback も除去する。

## 対象ファイル
- `runtime/scanner.py`(symlink skip)
- `runtime/builder.py`(resolved path の repo containment + `_node_id_for_path` の fallback 撤去 + `compute_edges` の同種 fallback も確認)
- `tests/test_phase_a_cli.py`(regression test 追加)

## 完了条件
- `scanner.py::list_indexable_files` 系で `is_symlink()` をスキップ
- `builder.py` で resolved path を `root_path.resolve()` に `relative_to` 出来ないものを skip
- `_node_id_for_path` は repo 外 target を返さず、escape 時は ValueError または skip
- regression test: 一時 fixture に repo 外を指す symlink を作り scan が skip することを assert(Windows で symlink 作成出来ない場合は `pytest.skip` で逃がす)
- `pytest -q` all green

## 非目標
- config 由来 path の containment(別タスクで対応)

## 着手前
- `codegraph context runtime/scanner.py` と `runtime/builder.py` で呼び出し関係を確認
- `C:\Codex\deep-research-report.md` の「symlink と resolved path の repo 外参照を拒否する」節を参照

## Result

scanner/builder で symlink skip + resolved path containment + _node_id_for_path の absolute fallback 撤去。regression test 追加。commit ee3bbc3。