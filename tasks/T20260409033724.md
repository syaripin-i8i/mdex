---
type: task
project: mdex
status: done
updated: 2026-04-09
---

# Task: mdex Phase1-align-2: indexer.py で JSON + SQLite 出力を実装

**ID**: T20260409033724
**Project**: mdex
**Priority**: high
**Status**: pending
**Created**: 2026-04-09T03:37:24+09:00

## Description

**実装先**: `C:\Codex\mdex\runtime\indexer.py`
**背景**: `docs/design.md` に合わせて、ノード/エッジ生成と JSON + SQLite 書き出しを indexer へ集約する。

## 実装対象

- `build_index(root: str, config: dict) -> dict`
- `write_json(index: dict, output_path: str) -> None`
- `write_sqlite(index: dict, db_path: str) -> None`
- SQLite は `nodes` / `edges` テーブルを生成

## 完了条件

- `python -m py_compile runtime/indexer.py` が通る
- `docs/` 対象ビルドで `mdex_index.json` と `mdex_index.db` が生成される
- `docs/proposal.md -> AGENT.md` の `links_to` エッジが含まれる

---
## 完了手順（Codex 定型）

### パス情報
- このファイル  : `C:\Codex\infra\tasks\pending\T20260409033724.md`
- 完了後の移動先: `C:\Codex\infra\tasks\done\T20260409033724.md`
- プロジェクト  : `C:\Codex\mdex`

### 完了時の操作
1. このファイルに Result セクションを追記する
2. ファイルを done/ に移動する
3. DB 更新は不要（done/ を取り込み）

## Result

- 実施内容: `C:\Codex\mdex\runtime\indexer.py` を新規作成し、`build_index(root, config)`・`write_json(index, output_path)`・`write_sqlite(index, db_path)` を実装。SQLite に `nodes` / `edges` テーブルを作成し、インデックス内容を書き込む処理を追加。
- 確認方法: `python -m py_compile C:\Codex\mdex\runtime\indexer.py` を実行して成功。`docs/` を対象に `mdex_index.json` と `mdex_index.db` を生成し、`docs/proposal.md -> AGENT.md` の `links_to` エッジ存在と DB の件数（nodes=2 / edges=10）を確認。
- 備考: `builder.py` の既存ノード生成ロジックを利用し、出力責務（JSON/SQLite）を indexer に集約した。
